import { httpInstanceNoAuth } from "../http/httpInstance";
import { handleHttpError } from "../helpers/helpers";

const EmailService = {
    sendEmail(fromName, fromEmail, text) {
        // api key - c75e57f3e8bad09616b4986962cb4063-a4da91cf-ff4b2ead
    },
};

export default EmailService;